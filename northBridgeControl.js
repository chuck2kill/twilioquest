// on récupère l'argument dans une variable
const argument = process.argv[2]

// si l'argument est 'EXTEND' on affiche le message
if (argument == 'EXTEND') {
    console.log('Extending bridge!')
}